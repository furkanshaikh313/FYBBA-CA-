#include<stdio.h>
#include<conio.h>

int main(){
    float len,bre,hei,sur;
    scanf("%f %f %f", &len &tbre &hei);
    sur = ((len*bre) * (bre * hei) * (len * hei));
    printf("the surface area is : %d", sur);
    return 0;
}